<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <script>
            let owner = new Array();
            let bank = new Array();
            let balance = 0;

            function register() {
                let userInfo = prompt('이름, 은행');
                if (userInfo.includes(',')) {
                    userInfoList = userInfo.split(',');
                    owner.push(userInfoList[0]);
                    bank.push(userInfo[1]);
                }
                else {
                    alert('올바르지 않은 형식');
                    register();
                }
            }
            
            function inquiry() {
                alert(balance);
            }

            function deposit() {
                
            }
        </script>
    </head>
</html>